import { View, Text } from 'react-native';
import React from 'react';

const Saved = () => {
    return (
        <View className="flex-1 items-center justify-center">
            <Text className="text-xl font-bold text-dark-200">Saved</Text>
        </View>
    );
};
export default Saved;